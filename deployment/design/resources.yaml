apiVersion: apps/v1
kind: Deployment
metadata:
  name: dss
spec:
  template:
    spec:
      containers:
        - name: dss
          resources:
            requests:
              cpu: 2
              memory: 10Gi
            limits:
              cpu: 2
              memory: 10Gi
